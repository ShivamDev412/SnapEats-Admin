const BASE_ROUTE = {
  AUTH: "/auth",
  Admin: "/Admin",
};

const ENDPOINTS = {
  LOGIN: "/login",
  LOGOUT: "/logout",
  REFRESH_TOKEN: "/refresh-token",
};

const BROWSER_ROUTE = {
  LOGIN: "/login",
  LOGOUT: "/logout",
  DASHBOARD: "/",
};

export { BASE_ROUTE, ENDPOINTS, BROWSER_ROUTE };
